"""Biological-application helpers for MOMO-GP.

Downstream analysis used by the notebooks in ``experiments/`` — gene/feature
relevance maps (``GRM``), clustering/evaluation metrics (``evaluation``), and
SEACells metacell construction (``SEACells_MOGP``). These are application code,
not part of the core model (see the ``momogp/`` package).
"""
